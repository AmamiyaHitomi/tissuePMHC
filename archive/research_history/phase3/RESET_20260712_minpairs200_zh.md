# Phase 3 重置记录（2026-07-12）

- 当前阶段：选择每个 task 的 pairs 数。
- 目标数据阈值：`min_pairs > 200`。
- 下一步：以该阈值重建 `data/mousePMHC/`，运行数据审计和 train-only 学习曲线；随后在新数据上重跑 E0、E1。
- 保留：小鼠 E0/E1 的代码和旧 `>500` OOF 结果，作为历史可追溯记录。
- 作废：旧 `>500` 小鼠 benchmark 上的 E2+、E14a、E29、其 smoke 运行、图和学习曲线结论；它们不得进入当前比较或报告。

本次重置不删除历史文件，避免破坏可追溯性。
